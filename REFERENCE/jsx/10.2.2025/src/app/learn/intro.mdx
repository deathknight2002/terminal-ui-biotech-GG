import { GradientText } from '@/components/ui/GradientText';

# <GradientText>Intro Module (MDX)</GradientText>

This MDX-powered module demonstrates rich content authoring.

## Features

- Use React components directly inside markdown.
- Future: embed diagrams, quizzes, callout blocks.

```tsx
function Example(){
  return <div className="glass p-2 text-xs">Aurora MDX Environment</div>
}
```

> Extend this with programmatic ingestion or a content pipeline.